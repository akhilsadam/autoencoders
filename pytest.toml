[pytest]
markers =
    benchmark: marks tests that perform training/evaluation benchmarks
    slow: marks tests as slow (deselect with '-m "not slow"')
python_files = ["*.py"]
python_classes = ["_TEST_*"]
python_functions = ["_test_*"]