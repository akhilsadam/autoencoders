name: rpn_encoder
fullname: rpn_encoder
params:
  root: ${paths.data_root}/rpn_encoder
  version: 1 

  seq_len: 50
  n_rpn: 8000

  batch_size: 32
  num_workers: 8
  test_workers: 8
  val_split: 20
  seed: 42